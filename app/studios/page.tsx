import { StudioDirectoryClient } from "@/components/discovery/StudioDirectoryClient";
export default function StudiosPage(): React.JSX.Element {
  return <main className="mx-auto w-full max-w-7xl px-5 py-12 sm:px-8"><StudioDirectoryClient /></main>;
}
