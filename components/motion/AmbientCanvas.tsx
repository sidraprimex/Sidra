"use client";
import { useEffect, useRef } from "react";
export function AmbientCanvas(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const ctx=c.getContext("2d");if(!ctx)return;let frame=0;let raf=0;const resize=()=>{const d=Math.min(devicePixelRatio,2);c.width=innerWidth*d;c.height=innerHeight*d;c.style.width=`${innerWidth}px`;c.style.height=`${innerHeight}px`;ctx.setTransform(d,0,0,d,0,0)};const draw=()=>{frame+=0.004;ctx.clearRect(0,0,innerWidth,innerHeight);const g=ctx.createRadialGradient(innerWidth*(.5+Math.sin(frame)*.08),innerHeight*.42,0,innerWidth*.5,innerHeight*.42,Math.max(innerWidth,innerHeight)*.65);g.addColorStop(0,"rgba(200,169,106,.16)");g.addColorStop(.42,"rgba(28,27,25,.08)");g.addColorStop(1,"rgba(11,11,11,0)");ctx.fillStyle=g;ctx.fillRect(0,0,innerWidth,innerHeight);raf=requestAnimationFrame(draw)};resize();draw();addEventListener("resize",resize);return()=>{cancelAnimationFrame(raf);removeEventListener("resize",resize)}},[]);return <canvas ref={ref} className="pointer-events-none absolute inset-0" aria-hidden="true"/>}
