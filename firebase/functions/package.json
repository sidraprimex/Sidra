{
  "name": "sidra-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "npm run build && vitest run --config vitest.config.ts && node --test lib/cmsDefaults.test.js lib/launchReadinessPolicy.test.js lib/sellerProvisioningCore.test.js"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.2",
    "@google-cloud/vision": "5.3.7"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^22.18.0",
    "vitest": "^3.2.4"
  }
}
