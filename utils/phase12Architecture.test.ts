import { describe,expect,it } from "vitest";
import fs from "node:fs";import path from "node:path";
const root=process.cwd();const read=(file:string)=>fs.readFileSync(path.join(root,file),"utf8");
describe("Phase 12 communication architecture",()=>{it("exports secure communication functions",()=>{const source=read("firebase/functions/src/index.ts");for(const name of ["createSupportTicket","sendSupportMessage","manageSupportTicket","markAllNotificationsRead"])expect(source).toContain(name);});it("routes writes through callable functions",()=>{const source=read("services/phase12CommunicationService.ts");expect(source).toContain("callSidraFunction");expect(source).not.toContain("setDocument(");});it("ships customer and admin support surfaces",()=>{for(const file of ["app/account/support/page.tsx","app/account/support/new/page.tsx","app/admin/support/page.tsx"])expect(fs.existsSync(path.join(root,file))).toBe(true);});it("keeps direct client writes disabled",()=>{const rules=read("firebase/rules/firestore.rules");expect(rules).toContain("match /supportTickets/{ticketId}");expect(rules).toContain("allow create, update, delete: if false;");});});
